% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_fitting.R
\name{qmbs}
\alias{qmbs}
\title{Fit Bayesian Models using qbrms (Alternative Interface)}
\usage{
qmbs(
  formula,
  data,
  family = gaussian(),
  prior = NULL,
  sample_prior = "no",
  quantile = 0.5,
  control.compute = list(dic = TRUE, waic = TRUE, cpo = TRUE),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{formula}{A model formula specifying the model structure.}

\item{data}{A data frame containing the variables in the model.}

\item{family}{A family object or character string specifying the response distribution.
Defaults to \code{gaussian()}.}

\item{prior}{Prior specification (currently not implemented).}

\item{sample_prior}{If "only", generate prior predictive samples instead of fitting the model.}

\item{quantile}{Quantile level for quantile regression (when family is asymmetric_laplace).}

\item{control.compute}{A list of control parameters for INLA computation.}

\item{verbose}{Logical; if TRUE, print progress information.}

\item{...}{Additional arguments passed to INLA.}
}
\value{
An object of class \code{qbrms_fit} containing the fitted model.
}
\description{
Alternative interface to \code{\link{qbrms}} with a shorter name.
This function provides the same functionality as \code{qbrms}.
}
\examples{
\dontrun{
# Simple linear regression
fit <- qmbs(y ~ x, data = data, family = gaussian())

# Prior predictive check
fit_prior <- qmbs(y ~ x, data = data, sample_prior = "only")
}
}
\seealso{
\code{\link{qbrms}}
}
